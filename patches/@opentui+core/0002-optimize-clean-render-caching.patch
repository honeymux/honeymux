diff --git a/src/Renderable.ts b/src/Renderable.ts
index f20d4be4..f936a701 100644
--- a/src/Renderable.ts
+++ b/src/Renderable.ts
@@ -231,6 +231,8 @@ export abstract class Renderable extends BaseRenderable {
 
   private _live: boolean = false
   protected _liveCount: number = 0
+  private readonly hasCustomUpdateHook: boolean
+  protected _customUpdateHookCount: number = 0
 
   private _sizeChangeListener: (() => void) | undefined = undefined
   private _mouseListener: ((event: MouseEvent) => void) | null = null
@@ -254,6 +256,9 @@ export abstract class Renderable extends BaseRenderable {
   private childrenPrimarySortDirty: boolean = true
   private childrenSortedByPrimaryAxis: Renderable[] = []
   private _shouldUpdateBefore: Set<Renderable> = new Set()
+  private cachedRenderCommands: RenderCommand[] = []
+  private cachedRenderCommandsValid: boolean = false
+  private subtreeDirty: boolean = true
 
   // Frame id of the last updateFromLayout(); -1 ensures the first call runs.
   private _lastLayoutFrame: number = -1
@@ -289,6 +294,8 @@ export abstract class Renderable extends BaseRenderable {
     this.buffered = options.buffered ?? false
     this._live = options.live ?? false
     this._liveCount = this._live && this._visible ? 1 : 0
+    this.hasCustomUpdateHook = this.onUpdate !== Renderable.prototype.onUpdate
+    this._customUpdateHookCount = this.hasCustomUpdateHook ? 1 : 0
     this._opacity = options.opacity !== undefined ? Math.max(0, Math.min(1, options.opacity)) : 1.0
 
     // TODO: use a global yoga config
@@ -486,6 +493,11 @@ export abstract class Renderable extends BaseRenderable {
     this.parent?.propagateLiveCount(delta)
   }
 
+  protected propagateCustomUpdateHookCount(delta: number): void {
+    this._customUpdateHookCount += delta
+    this.parent?.propagateCustomUpdateHookCount(delta)
+  }
+
   public handleKeyPress?(key: KeyEvent): boolean
   public handlePaste?(event: PasteEvent): void
 
@@ -502,9 +514,19 @@ export abstract class Renderable extends BaseRenderable {
 
   public requestRender() {
     this.markDirty()
+    this.markSubtreeDirty()
     this._ctx.requestRender()
   }
 
+  protected markSubtreeDirty(): void {
+    let node: Renderable | null = this
+    while (node) {
+      node.subtreeDirty = true
+      node.cachedRenderCommandsValid = false
+      node = node.parent
+    }
+  }
+
   public get translateX(): number {
     return this._translateX
   }
@@ -536,12 +558,12 @@ export abstract class Renderable extends BaseRenderable {
   // Use the cached parent screen position plus this node's current local offset.
   // That keeps culling/sorting in sync even before this node refreshes _screenX/Y.
   public get screenX(): number {
-    const parentScreenX = this.parent ? this.parent._screenX : 0
+    const parentScreenX = this.parent ? this.parent.screenX : 0
     return parentScreenX + this._x + this._translateX
   }
 
   public get screenY(): number {
-    const parentScreenY = this.parent ? this.parent._screenY : 0
+    const parentScreenY = this.parent ? this.parent.screenY : 0
     return parentScreenY + this._y + this._translateY
   }
 
@@ -1205,6 +1227,9 @@ export abstract class Renderable extends BaseRenderable {
       if (renderable._liveCount > 0) {
         this.propagateLiveCount(renderable._liveCount)
       }
+      if (renderable._customUpdateHookCount > 0) {
+        this.propagateCustomUpdateHookCount(renderable._customUpdateHookCount)
+      }
     }
 
     const childLayoutNode = renderable.getLayoutNode()
@@ -1282,6 +1307,9 @@ export abstract class Renderable extends BaseRenderable {
       if (renderable._liveCount > 0) {
         this.propagateLiveCount(renderable._liveCount)
       }
+      if (renderable._customUpdateHookCount > 0) {
+        this.propagateCustomUpdateHookCount(renderable._customUpdateHookCount)
+      }
     }
 
     this.childrenPrimarySortDirty = true
@@ -1315,6 +1343,9 @@ export abstract class Renderable extends BaseRenderable {
         if (obj._liveCount > 0) {
           this.propagateLiveCount(-obj._liveCount)
         }
+        if (obj._customUpdateHookCount > 0) {
+          this.propagateCustomUpdateHookCount(-obj._customUpdateHookCount)
+        }
 
         const childLayoutNode = obj.getLayoutNode()
         this.yogaNode.removeChild(childLayoutNode)
@@ -1353,9 +1384,18 @@ export abstract class Renderable extends BaseRenderable {
     return this._childrenInLayoutOrder.length
   }
 
-  public updateLayout(deltaTime: number, renderList: RenderCommand[] = []): void {
+  public updateLayout(
+    deltaTime: number,
+    renderList: RenderCommand[] = [],
+    ancestorLayoutChanged: boolean = false,
+  ): void {
     if (!this.visible) return
 
+    if (this.canReuseRenderCommands(ancestorLayoutChanged)) {
+      renderList.push(...this.cachedRenderCommands)
+      return
+    }
+
     this.onUpdate(deltaTime)
 
     // If destroyed during onUpdate, don't add to render list
@@ -1386,6 +1426,8 @@ export abstract class Renderable extends BaseRenderable {
 
     // Push opacity BEFORE rendering this element so it affects this element and all children
     const shouldPushOpacity = this._opacity < 1.0
+    const cacheStart = renderList.length
+
     if (shouldPushOpacity) {
       renderList.push({ action: "pushOpacity", opacity: this._opacity })
     }
@@ -1411,7 +1453,7 @@ export abstract class Renderable extends BaseRenderable {
     // unless a subclass actually performs viewport/style-based child filtering.
     if (!this._hasVisibleChildFilter()) {
       for (const child of this._childrenInZIndexOrder) {
-        child.updateLayout(deltaTime, renderList)
+        child.updateLayout(deltaTime, renderList, ancestorLayoutChanged)
       }
     } else {
       // Refresh every child's layout before culling reads their screen
@@ -1426,7 +1468,7 @@ export abstract class Renderable extends BaseRenderable {
       const visibleChildSet = new Set(visibleChildren)
       for (const child of this._childrenInZIndexOrder) {
         if (!visibleChildSet.has(child.num)) continue
-        child.updateLayout(deltaTime, renderList)
+        child.updateLayout(deltaTime, renderList, ancestorLayoutChanged)
       }
     }
 
@@ -1436,6 +1478,8 @@ export abstract class Renderable extends BaseRenderable {
     if (shouldPushOpacity) {
       renderList.push({ action: "popOpacity" })
     }
+
+    this.updateCachedRenderCommands(renderList, cacheStart, ancestorLayoutChanged)
   }
 
   public render(buffer: OptimizedBuffer, deltaTime: number): void {
@@ -1454,10 +1498,10 @@ export abstract class Renderable extends BaseRenderable {
       this.renderAfter.call(this, renderBuffer, deltaTime)
     }
 
-    // Hooks may move the renderable mid-frame, so sample the cached absolute
+    // Hooks may move the renderable mid-frame, so sample the absolute
     // position after they run before hit-grid writes or framebuffer compositing.
-    const screenX = this._screenX
-    const screenY = this._screenY
+    const screenX = this.screenX
+    const screenY = this.screenY
 
     this.markClean()
     this._ctx.addToHitGrid(screenX, screenY, this.width, this.height, this.num)
@@ -1477,6 +1521,56 @@ export abstract class Renderable extends BaseRenderable {
     return this._childrenInZIndexOrder.map((child) => child.num)
   }
 
+  private canReuseRenderCommands(ancestorLayoutChanged: boolean): boolean {
+    if (ancestorLayoutChanged) {
+      return false
+    }
+
+    if (
+      this.subtreeDirty ||
+      this._liveCount > 0 ||
+      this._customUpdateHookCount > 0 ||
+      this._shouldUpdateBefore.size > 0 ||
+      this.needsZIndexSort
+    ) {
+      return false
+    }
+
+    return this.cachedRenderCommandsValid
+  }
+
+  private updateCachedRenderCommands(
+    renderList: RenderCommand[],
+    cacheStart: number,
+    ancestorLayoutChanged: boolean,
+  ): void {
+    this.subtreeDirty = false
+
+    if (
+      ancestorLayoutChanged ||
+      this._liveCount > 0 ||
+      this._customUpdateHookCount > 0 ||
+      this._shouldUpdateBefore.size > 0 ||
+      this.needsZIndexSort
+    ) {
+      this.cachedRenderCommandsValid = false
+      this.cachedRenderCommands = []
+      return
+    }
+
+    const subtreeCommands = renderList.slice(cacheStart)
+    if (
+      subtreeCommands.some((command) => command.action === "pushScissorRect" || command.action === "popScissorRect")
+    ) {
+      this.cachedRenderCommandsValid = false
+      this.cachedRenderCommands = []
+      return
+    }
+
+    this.cachedRenderCommands = subtreeCommands
+    this.cachedRenderCommandsValid = true
+  }
+
   protected onUpdate(deltaTime: number): void {
     // Default implementation: do nothing
     // Override this method to provide custom rendering
@@ -1489,8 +1583,8 @@ export abstract class Renderable extends BaseRenderable {
     height: number
   } {
     return {
-      x: this.buffered ? 0 : this._screenX,
-      y: this.buffered ? 0 : this._screenY,
+      x: this.buffered ? 0 : this.screenX,
+      y: this.buffered ? 0 : this.screenY,
       width: this.width,
       height: this.height,
     }
@@ -1741,13 +1835,14 @@ export class RootRenderable extends Renderable {
     // but that's only possible if we move the layout tree to native.
 
     // 1. Calculate layout from root
-    if (this.yogaNode.isDirty()) {
+    const layoutChanged = this.yogaNode.isDirty()
+    if (layoutChanged) {
       this.calculateLayout()
     }
 
     // 2. Update layout throughout the tree and collect render list
     this.renderList.length = 0
-    this.updateLayout(deltaTime, this.renderList)
+    this.updateLayout(deltaTime, this.renderList, layoutChanged)
 
     // 3. Render all collected renderables
     this._ctx.clearHitGridScissorRects()
diff --git a/src/renderables/EditBufferRenderable.ts b/src/renderables/EditBufferRenderable.ts
index 22da4cc3..00b0068e 100644
--- a/src/renderables/EditBufferRenderable.ts
+++ b/src/renderables/EditBufferRenderable.ts
@@ -982,10 +982,8 @@ export abstract class EditBufferRenderable extends Renderable implements LineInf
   render(buffer: OptimizedBuffer, deltaTime: number): void {
     if (!this.visible) return
     if (this.isDestroyed) return
-    // Editor rendering/cursor placement reads absolute coordinates multiple
-    // times per frame, so it benefits from the same cached screen position.
-    const screenX = this._screenX
-    const screenY = this._screenY
+    const screenX = this.screenX
+    const screenY = this.screenY
 
     this.markClean()
     this._ctx.addToHitGrid(screenX, screenY, this.width, this.height, this.num)
@@ -995,15 +993,15 @@ export abstract class EditBufferRenderable extends Renderable implements LineInf
   }
 
   protected renderSelf(buffer: OptimizedBuffer): void {
-    buffer.drawEditorView(this.editorView, this._screenX, this._screenY)
+    buffer.drawEditorView(this.editorView, this.screenX, this.screenY)
   }
 
   protected renderCursor(buffer: OptimizedBuffer): void {
     if (!this._showCursor || !this._focused) return
 
     const visualCursor = this.editorView.getVisualCursor()
-    const screenX = this._screenX
-    const screenY = this._screenY
+    const screenX = this.screenX
+    const screenY = this.screenY
 
     const cursorX = screenX + visualCursor.visualCol + 1 // +1 for 1-based terminal coords
     const cursorY = screenY + visualCursor.visualRow + 1 // +1 for 1-based terminal coords
diff --git a/src/renderables/Text.test.ts b/src/renderables/Text.test.ts
index d38ffb7e..178a5531 100644
--- a/src/renderables/Text.test.ts
+++ b/src/renderables/Text.test.ts
@@ -84,6 +84,40 @@ describe("TextRenderable Selection", () => {
     })
   })
 
+  describe("Buffered Rendering", () => {
+    it("reuses the framebuffer for clean buffered text across unrelated rerenders", async () => {
+      const { text } = await createTextRenderable(currentRenderer, {
+        content: "cached",
+        buffered: true,
+      })
+
+      const tick = new TextRenderable(currentRenderer, {
+        id: "tick",
+        content: "a",
+        position: "absolute",
+        left: 0,
+        top: 2,
+      })
+      currentRenderer.root.add(tick)
+      await renderOnce()
+
+      const frameBuffer = (text as any).frameBuffer
+      expect(frameBuffer).toBeTruthy()
+
+      let drawCount = 0
+      const originalDrawTextBuffer = frameBuffer.drawTextBuffer.bind(frameBuffer)
+      frameBuffer.drawTextBuffer = ((...args: unknown[]) => {
+        drawCount++
+        return originalDrawTextBuffer(...(args as Parameters<typeof originalDrawTextBuffer>))
+      }) as typeof frameBuffer.drawTextBuffer
+
+      tick.content = "b"
+      await renderOnce()
+
+      expect(drawCount).toBe(0)
+    })
+  })
+
   describe("Basic Selection Flow", () => {
     it("should handle selection from start to end", async () => {
       const { text } = await createTextRenderable(currentRenderer, {
diff --git a/src/renderables/TextBufferRenderable.ts b/src/renderables/TextBufferRenderable.ts
index b2659b1f..7e0ddcc7 100644
--- a/src/renderables/TextBufferRenderable.ts
+++ b/src/renderables/TextBufferRenderable.ts
@@ -38,6 +38,7 @@ export abstract class TextBufferRenderable extends Renderable implements LineInf
   protected _scrollY: number = 0
   protected _truncate: boolean = false
   protected _firstLineOffset: number = 0
+  private frameBufferRendered: boolean = false
 
   protected textBuffer: TextBuffer
   protected textBufferView: TextBufferView
@@ -480,25 +481,45 @@ export abstract class TextBufferRenderable extends Renderable implements LineInf
 
   render(buffer: OptimizedBuffer, deltaTime: number): void {
     if (!this.visible) return
-    // Text views do enough per-frame work that avoiding recursive x/y lookups is
-    // measurable; use the layout cache for hit-grid and draw entry points.
-    const screenX = this._screenX
-    const screenY = this._screenY
-
-    this.markClean()
-    this._ctx.addToHitGrid(screenX, screenY, this.width, this.height, this.num)
-
-    this.renderSelf(buffer)
+    // Use live screen coordinates here because render-command reuse can skip
+    // refreshing cached descendant positions after ancestor-only translates.
+    const screenX = this.screenX
+    const screenY = this.screenY
 
     if (this.buffered && this.frameBuffer) {
+      if (!this.frameBufferRendered || this.isDirty || this.live) {
+        this.frameBuffer.clear(this.bg)
+        this.renderSelf(this.frameBuffer)
+        this.frameBufferRendered = true
+      }
+
       buffer.drawFrameBuffer(screenX, screenY, this.frameBuffer)
+    } else {
+      this.renderSelf(buffer)
     }
+
+    this.markClean()
+    this._ctx.addToHitGrid(screenX, screenY, this.width, this.height, this.num)
   }
 
   protected renderSelf(buffer: OptimizedBuffer): void {
+    const { x, y } = this.getRenderOrigin(buffer)
     if (this.textBuffer.ptr) {
-      buffer.drawTextBuffer(this.textBufferView, this._screenX, this._screenY)
+      buffer.drawTextBuffer(this.textBufferView, x, y)
+    }
+  }
+
+  protected handleFrameBufferResize(width: number, height: number): void {
+    super.handleFrameBufferResize(width, height)
+    this.frameBufferRendered = false
+  }
+
+  private getRenderOrigin(buffer: OptimizedBuffer): { x: number; y: number } {
+    if (this.buffered && this.frameBuffer === buffer) {
+      return { x: 0, y: 0 }
     }
+
+    return { x: this.screenX, y: this.screenY }
   }
 
   destroy(): void {
diff --git a/src/tests/render-command-cache.test.ts b/src/tests/render-command-cache.test.ts
new file mode 100644
index 00000000..6d0ab02d
--- /dev/null
+++ b/src/tests/render-command-cache.test.ts
@@ -0,0 +1,90 @@
+import { afterEach, beforeEach, describe, expect, it } from "bun:test"
+import type { RenderCommand } from "../Renderable.js"
+import { BoxRenderable } from "../renderables/Box.js"
+import { TextRenderable } from "../renderables/Text.js"
+import { createTestRenderer, type TestRenderer } from "../testing/test-renderer.js"
+
+let currentRenderer: TestRenderer
+let renderOnce: () => Promise<void>
+
+class CountingLeafBox extends BoxRenderable {
+  public updateLayoutCalls = 0
+
+  public override updateLayout(
+    deltaTime: number,
+    renderList: RenderCommand[] = [],
+    ancestorLayoutChanged: boolean = false,
+  ): void {
+    this.updateLayoutCalls++
+    super.updateLayout(deltaTime, renderList, ancestorLayoutChanged)
+  }
+}
+
+describe("Render command cache", () => {
+  beforeEach(async () => {
+    ;({ renderer: currentRenderer, renderOnce } = await createTestRenderer({
+      width: 20,
+      height: 6,
+    }))
+  })
+
+  afterEach(() => {
+    currentRenderer.destroy()
+  })
+
+  it("skips clean static descendants on unrelated rerenders", async () => {
+    const staticParent = new BoxRenderable(currentRenderer, {
+      id: "static-parent",
+      position: "absolute",
+      left: 0,
+      top: 0,
+      width: 12,
+      height: 3,
+      border: false,
+    })
+    const countedLeaf = new CountingLeafBox(currentRenderer, {
+      id: "counted-leaf",
+      position: "absolute",
+      left: 1,
+      top: 1,
+      width: 4,
+      height: 1,
+      border: false,
+    })
+    countedLeaf.add(
+      new TextRenderable(currentRenderer, {
+        id: "label",
+        content: "leaf",
+        position: "absolute",
+        left: 0,
+        top: 0,
+      }),
+    )
+    staticParent.add(countedLeaf)
+
+    const tick = new TextRenderable(currentRenderer, {
+      id: "tick",
+      content: "a",
+      position: "absolute",
+      left: 0,
+      top: 4,
+    })
+
+    currentRenderer.root.add(staticParent)
+    currentRenderer.root.add(tick)
+
+    await renderOnce()
+    const initialCalls = countedLeaf.updateLayoutCalls
+    expect(initialCalls).toBeGreaterThan(0)
+
+    tick.requestRender()
+    await renderOnce()
+    const warmedCalls = countedLeaf.updateLayoutCalls
+
+    tick.requestRender()
+    await renderOnce()
+
+    expect(warmedCalls).toBeGreaterThan(initialCalls)
+    expect(countedLeaf.updateLayoutCalls).toBe(warmedCalls)
+  })
+})
